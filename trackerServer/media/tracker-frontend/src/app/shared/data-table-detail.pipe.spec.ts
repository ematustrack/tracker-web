import { DataTableDetailPipe } from './data-table-detail.pipe';

describe('DataTableDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new DataTableDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
